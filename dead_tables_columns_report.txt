Discuz! X - pre_forum_ and pre_common_ Dead Tables & Columns Report
=================================================================

1. Suspected Dead Tables
------------------------
These tables have no exact string matches for their class name (`table_xxx`), `DB::table('xxx')`, `C::t('xxx')`, or raw SQL table name in the PHP source files.
Note: Tables ending in a number (like `pre_forum_attachment_0`) are dynamically sharded and are actively used.

- pre_common_connect_guest
- pre_common_smslog_archive
- pre_common_member_connect
- pre_forum_post_moderate
- pre_forum_post_history
- pre_forum_thread_moderate


2. Suspected Dead Columns in Active Tables
------------------------------------------
These column names never appear as exact words in the PHP source files.

- Table `pre_common_member`:
  * `notifysound`
- Table `pre_common_member_security`:
  * `newvalue`
- Table `pre_common_searchindex`:
  * `threadsortid`
- Table `pre_forum_creditslog`:
  * `fromto`
  * `sendcredits`
  * `receivecredits`
- Table `pre_forum_post`:
  * `premsg`


Disclaimer:
This report is generated by searching for exact string matches in the PHP code.
Some false positives may exist if table/column names are built dynamically.
